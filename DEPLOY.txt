APPLIED AI ROADMAP — NOTIFICATION PATCH (BUGFIXED)

Replace these files in the GitHub repository:
1. index.html
2. server.js  (unchanged from your upload — no bugs found in it)

You do NOT need to put the Gemini API key in GitHub.
Keep GEMINI_API_KEY in Render -> Environment Variables (already set, per you).

After GitHub push:
1. Wait for Render auto-deploy.
2. Open /health on the Render URL. Check geminiConfigured:true.
3. Open the site, tap the 🔔 bell, then tap Refresh in Notifications.

What changed in this pass:

BUGS FIXED
- index.html CSS: `.opp-card{...}` was never closed before `.opp-delete-toolbar{...}`
  started, which silently swallowed most of the notification-panel CSS (delete
  toolbar, select-all, section labels, rank badges, etc.) into one broken rule.
  Fixed by properly closing/reopening the rules. Also fixed a second orphaned
  CSS block (min-width/max-width/overflow with no selector) that was dropped
  the same way — restored as its own `.opp-card{}` rule.
- Client-side ranking (`oppRank`) used a different formula than the server's
  dynamic Top-10 ranking (score + deadline urgency). Fixed so the browser's
  Top 10 always matches what the backend currently considers best — this is
  what makes "a new great opportunity takes a Top-10 spot" actually visible.
- The browser's local notification list merged forever and never dropped
  items the server had already ranked out of its Top 30 (especially ones
  with no deadline, which never expire). Fixed: each successful refresh now
  mirrors the server's current list exactly (only "read" state carried over),
  so old/weaker items don't linger indefinitely and skew the ranking.

NEW BEHAVIOR (as requested)
- Top 10 is shown expanded by default, exactly like before — genuinely
  dynamic, recalculated every refresh, so a stronger new opportunity really
  can take a Top-10 spot.
- Items 11-30 are now collapsed behind a "👁 सर्व दाखवा / See All" button
  under the Top 10 section. Tapping it expands the remaining ~20; tapping
  again ("▲ लपवा / Hide") collapses them.

UNCHANGED (already correct, verified — no change needed)
- Dismiss is temporary only: a dismissed notification is not permanently
  blocked, and can reappear on a later refresh if the server still lists it,
  alongside any genuinely new opportunities.
- Interview Mode / Test Mode: already removed from this version.
- Full roadmap document modal: already present in this version.
