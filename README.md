# Backend

हा भाग live opportunity discovery आणि `/opportunities.json` feed चालवतो.

`server.js` parent folder मधील `Mobile/index.html` serve करतो, त्यामुळे पूर्ण package deploy केल्यावर frontend + backend same-origin राहतात.
